# VM1 Host-only address. This binds SonarQube only to the private lab network.
SONAR_HOST_IP=192.168.1.6

# Generate a unique value on VM1; never commit the real .env file.
SONAR_DB_PASSWORD=replace-with-a-random-password
